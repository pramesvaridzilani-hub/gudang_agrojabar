{
  "name": "gudang",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run db:setup",
    "db:setup": "cd backend && npx prisma generate && npx prisma db push && npm run db:seed",
    "dev": "concurrently --kill-others-on-fail --names \"BACKEND,FRONTEND\" --prefix-colors \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npx wait-on tcp:5005 && npm run dev --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
